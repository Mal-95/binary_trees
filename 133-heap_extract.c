#include "binary_trees.h"

/**
 * heap_extract - extracts the root node of a Max Binary Heap
 * @root: point twice to the root node of heap
 *
 * Return: the value placed in the root node
 *         0 if failure
 */
int heap_extract(heap_t **root)
{

}
